<config_plugin>
    <!--
        order of configuration is:
          1.) project name and step name matches
          2.) step name matches and project is *
          3.) project name matches and step name is *
          4.) project name and step name are *
	-->
    
    <config>
        <!-- which projects to use for (can be more then one, otherwise use *) -->
        <project>*</project>
        <step>*</step>
        
        <!-- Configuration for structure element type -->
        <type>*</type>
        
        <!-- Configuration for filename string -->
        <infix>_backprint</infix>
        <!--    if true overwrites the existing Images and creates new entries.
                If false add new entries and don't touch old entries. -->
        <overwrite>false</overwrite>
    </config>

</config_plugin>
